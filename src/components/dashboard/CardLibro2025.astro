---
import { Image } from 'astro:assets';
function fetchConcorrenteBySlug(concorrente: string) {
  throw new Error('Function not implemented.');
}
export async function getStaticProps({
  params,
}: {
  params: { concorrente: string };
}) {
  const concorrenteData = await fetchConcorrenteBySlug(params.concorrente); // Fetch concorrente data by slug

  return {
    props: { concorrente: concorrenteData },
  };
}

const { libro } = Astro.props;
---

<div
  class='max-w-sm bg-white border border-gray-200 rounded-lg shadow overflow-hidden'>
  <Image
    src={libro.data.cover}
    class='w-full object-cover'
    alt={libro.data.alt}
    width={400}
    height={400}
  />
  <div class='p-4'>
    <h2 class='text-lg font-semibold text-gray-800'>{libro.data.title}</h2>
    <p class='text-sm text-gray-500 mb-2'>{libro.data.author}</p>
    <p class='text-sm text-gray-700 mb-4'>
      {libro.data.body}
    </p>
    <p class='text-sm text-gray-700 mb-4'>
      {libro.data.tag}
    </p>
    <a
      href='#'
      class='inline-block px-3 py-2 text-sm text-white bg-blue-500 rounded hover:bg-blue-600 transition-colors'>
      Learn More
    </a>
  </div>
</div>
