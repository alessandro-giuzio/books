<header>
  <nav
    class='flex mx-auto items-center border shadow-lg border-white justify-evenly w-full gap-2 py-4 px-4 text-xs'>
    <!-- <a
      class='py-2 font-bold uppercase'
      href='/'
      target='_blank'
      rel='noopener noreferrer'
    >
      Home
    </a> -->
    <!--  <a href='/'>
      <span class='relative flex h-4 w-4'>
        <span
          class='animate-ping ap absolute inline-flex h-full w-full rounded-full'
        ></span>
        <span class='ap relative inline-flex rounded-full h-4 w-4'></span>
      </span>
    </a> -->
    <!--    <a
      class='py-2 font-bold uppercase'
      href='/libros'
      rel='noopener noreferrer'>
      Libros
    </a> -->

    <select data-choose-theme class='py-1 px-1 rounded-md text-lg'>
      <option value=''>Theme</option>
      <!-- <option value='default' class=''>Default</option> -->
      <option value='alessandro'>Alessandro</option>
      <option value='amaia'>Amaia</option>
      <option value='asier'>Asier</option>
      <option value='irene'>Irene</option>
      <option value='luca'>Luca</option>
      <option value='malen'>Malen</option>
      <option value='oihana'>Oihana</option>
      <option value='unai'>Unai</option>
    </select>
    <!-- <a href='/'>
      <span class='relative flex h-4 w-4'>
        <span
          class='animate-ping ap absolute inline-flex h-full w-full rounded-full'
        ></span>
        <span class='ap relative inline-flex rounded-full h-4 w-4'></span>
      </span>
    </a> -->
  </nav>
</header>

<style>
  nav {
    background-color: var(--bg-header);
    border: 2px solid var(--nav-border);
    box-shadow: 0px 0px 30px var(--nav-border);
    animation: pulse 2s infinite;
    backdrop-filter: blur(60px);

    /* background-image: var(--nav-gradient); */
  }
  a {
    color: var(--text);
    font-size: 1.55rem;
    font-weight: 900;
    line-height: 1.8;
  }

  select {
    background-color: var(--bg-card);
    color: var(--text);
  }
  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 var(--nav-border);
    }
    50% {
      box-shadow: 0 0 10px 10px var(--nav-border);
    }
    100% {
      box-shadow: 0 0 0 0 var(--nav-border);
    }
  }

  span.ap {
    background-color: var(--text);
    opacity: 0.75;
  }
</style>
